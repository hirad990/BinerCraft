import { useEffect,useState } from 'react'
import { Link } from 'react-router-dom'
const Icon=({name,size=22})=>{const p={wallet:<><rect x="3" y="6" width="18" height="14" rx="2"/><path d="M16 10h5v5h-5a2.5 2.5 0 0 1 0-5Z"/><path d="M6 3h11a2 2 0 0 1 2 2"/></>,arrow:<><path d="M5 12h14M13 6l6 6-6 6"/></>};return <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">{p[name]}</svg>}
export default function WalletPage(){const[wallet,setWallet]=useState(null);const[loading,setLoading]=useState(true);useEffect(()=>{const u=JSON.parse(localStorage.getItem('user')||'null');if(!u?.token){setLoading(false);return}fetch(`/api/wallet/${encodeURIComponent(u.id)}`,{headers:{Authorization:`Bearer ${u.token}`}}).then(r=>r.ok?r.json():Promise.reject()).then(x=>setWallet(Number(x.wallet||0))).catch(()=>setWallet(Number(u.wallet||0))).finally(()=>setLoading(false))},[]);return <main className="container mx-auto max-w-4xl px-4 py-10 sm:py-14"><Link to="/profile" className="inline-flex items-center gap-2 text-sm font-bold text-primary-600 dark:text-primary-400"><Icon name="arrow" size={17}/> پروفایل</Link><section className="mt-6 overflow-hidden rounded-[2rem] border border-glass-border bg-gradient-to-br from-primary-600/15 via-background to-purple-600/10 p-8 shadow-xl"><div className="flex items-center gap-4"><span className="rounded-2xl bg-primary-500/10 p-4 text-primary-500"><Icon name="wallet" size={30}/></span><div><h1 className="text-3xl font-black">کیف پول</h1><p className="mt-1 text-text-secondary">موجودی کیف پول حساب شما</p></div></div><div className="mt-10 rounded-3xl border border-glass-border bg-background/50 p-7 text-center"><p className="text-sm text-text-secondary">موجودی فعلی</p><p className="mt-3 text-4xl font-black text-primary-600 dark:text-primary-400">{loading?'—':wallet.toLocaleString('fa-IR')} تومان</p></div></section></main>}
