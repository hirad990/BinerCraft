import { useState } from 'react'
import { motion, useReducedMotion } from 'framer-motion'
import axios from 'axios'
import { Link, useNavigate } from 'react-router-dom'
const Icon=({size=52})=><svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.7" strokeLinecap="round" strokeLinejoin="round"><path d="m12 3 7 4v5c0 4.5-3 7.5-7 9-4-1.5-7-4.5-7-9V7l7-4Z"/><path d="m9 12 2 2 4-4"/></svg>
export default function LoginPage(){const[identifier,setIdentifier]=useState('');const[password,setPassword]=useState('');const[loading,setLoading]=useState(false);const[error,setError]=useState('');const navigate=useNavigate();const reduce=useReducedMotion();const submit=async e=>{e.preventDefault();setError('');setLoading(true);try{const{data}=await axios.post('/api/users/login',{identifier,password});localStorage.setItem('user',JSON.stringify({...data.user,token:data.token||null}));if(data.token)localStorage.setItem('binercraft_user_token',data.token);window.dispatchEvent(new Event('binercraft-auth-changed'));navigate('/')}catch(err){setError(err.response?.data?.error||'ورود انجام نشد. دوباره تلاش کنید.')}finally{setLoading(false)}};return <main className="container mx-auto max-w-5xl px-4 py-10 sm:py-16"><motion.div initial={reduce?false:{opacity:0,scale:.98}} animate={{opacity:1,scale:1}} className="mx-auto grid overflow-hidden rounded-3xl border border-glass-border bg-glass-bg shadow-2xl md:grid-cols-2"><div className="hidden bg-primary-600 p-10 text-white md:flex md:flex-col md:justify-between"><div><Icon/><h2 className="mt-8 text-4xl font-black">به BinerCraft برگرد.</h2><p className="mt-4 leading-8 text-white/75">حساب تو، فروشگاه و تجربه بازی در یکجا.</p></div><div className="rounded-2xl border border-white/15 bg-white/10 p-4 text-sm">حساب شما با امنیت بهتر مدیریت می‌شود.</div></div><div className="p-6 sm:p-10"><span className="text-sm font-bold text-primary-600 dark:text-primary-400">WELCOME BACK</span><h1 className="mt-2 text-3xl font-black">ورود به حساب</h1><p className="mt-2 text-text-secondary">نام کاربری یا ایمیل و رمزت را وارد کن.</p><form onSubmit={submit} className="mt-7 space-y-5"><label className="block"><span className="text-sm font-bold">نام کاربری یا ایمیل</span><input value={identifier} onChange={e=>setIdentifier(e.target.value)} required autoComplete="username" className="input-field mt-2 w-full"/></label><label className="block"><span className="text-sm font-bold">رمز عبور</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password" className="input-field mt-2 w-full"/></label>{error&&<div role="alert" className="rounded-xl border border-red-500/20 bg-red-500/10 p-3 text-sm text-red-500">{error}</div>}<button disabled={loading} className="w-full rounded-xl bg-primary-600 px-4 py-3.5 font-black text-white transition hover:bg-primary-700 disabled:opacity-50">{loading?'در حال ورود…':'ورود به حساب'}</button></form><p className="mt-6 text-sm text-text-secondary">حساب نداری؟ <Link to="/register" className="font-bold text-primary-600 dark:text-primary-400">ثبت‌نام کن</Link></p></div></motion.div></main>}
